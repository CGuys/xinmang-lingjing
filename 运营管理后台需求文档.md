---

# 文件三：`ADMIN_PRD.md`（运营管理后台需求文档）

```markdown
# 《心芒灵境》Web 运营管理后台需求规格说明书 (PRD)

## 1. 文档概述
* **定位**：为运营人员和开发者提供全平台系统参数配置、AI Prompt 编排、数据漏斗观测与用户风控的后台管理系统。
* **技术栈**：Vue 3 + Vite + Element Plus + Pinia + ECharts。
* **核心目标**：实现无需发版即可动态控制小程序业务开关、监控裂变效率并微调大模型回复质量。

---

## 2. 功能模块设计

### 2.1 数据概览仪表盘 (Dashboard)
1. **核心 KPI 卡片**：
   * 今日活跃用户 (DAU) 与累计用户数。
   * 今日抽牌总次数（免费抽牌数 vs 裂变补能抽牌数）。
   * 裂变系数（K-factor = 受邀进入用户数 / 分享卡片发出数）。
   * 当日大模型 Token 消耗量估算及成本折算。
2. **图表展示**：
   * 过去 30 天用户新增与留存趋势折线图。
   * 每日抽牌时间分布热力图（观测白领晚间高峰）。

### 2.2 业务策略中控台 (System Config)
提供动态表单配置系统关键运行参数，保存后即时写入 Redis：
1. **能量与裂变规则**：
   * `daily_free_limit`：每日自然日赠送免费次数（默认：`1`）。
   * `share_reward_limit`：单日分享最高获得能量上限（默认：`3`）。
   * `share_reward_enabled`：是否开启分享奖励补能（布尔开关）。
2. **商业化灰度开关**：
   * `ad_reward_enabled`：是否在小程序端显示“看激励视频补能”按钮（支持在审核通过后一键开启）。
   * `ad_unit_id`：微信激励视频广告位 ID。
3. **微信审核模式 (`is_in_review`)**：
   * 开关开启时：小程序端关闭所有自定义文本输入框，流式解读返回预设的纯正向文学鸡汤，避开微信人工机审拦截。

### 2.3 AI 模型与 Prompt 编排中心
1. **大模型网关参数配置**：
   * 服务商切换下拉选框（DeepSeek / 智谱 GLM / MiniMax / 聚合中转）。
   * API Key、Base URL、Model Name 输入配置。
   * 模型温度（Temperature）、Top_P、Max_Tokens 滑块配置。
2. **Prompt 模板版本控制**：
   * System Prompt 编辑框（带高亮）。
   * 内置占位符说明：`{card_name}` (卡牌名称)、`{orientation}` (正逆位)、`{question}` (用户输入)。
   * **在线调试沙盒**：在后台直接模拟抽牌并触发模型输出，测试打字机输出流畅度及是否存在违禁词。

### 2.4 内容与风控管理
1. **卡牌库维护**：
   * 查看 78 张卡牌的中文名、英文名、预设心理学关键词及 CDN 插画预览。
2. **违禁词词库配置**：
   * 自定义敏感词字典（如迷信类词汇：改运、断命、看相等），前端命中时直接弹窗提示，不向后端发出请求。
3. **用户管理**：
   * 列表查看用户 OpenID、注册时间、当前能量余额。
   * 动作：手动补充/扣除能量、封禁作弊账号（黑名单屏蔽请求）。

---

## 3. 页面结构与路由规划

```text
/admin
├── /login              # 管理员账号密码登录页
├── /dashboard          # 核心运营与裂变数据指标总览
├── /strategy           # 业务策略配置 (开关、能量上限、审核模式)
├── /ai-orchestration   # Prompt 模板设计、模型网关与沙盒测试
├── /cards              # 78 张卡牌基础档案与资源库
├── /sensitive-words    # 敏感词过滤库管理
└── /users              # 用户列表、日志与能量状态管理


以下为您将**小程序端（用户端）**、Web 后台（管理端）**与**服务端（API 网关与业务）拆分独立后的三份结构化 Markdown 需求规格说明书（PRD）。您可以直接将其分别保存为 `.md` 文件用于开发与协作。

---

# 文件一：`MINIPROGRAM_PRD.md`（小程序端需求文档）

```markdown
# 《心芒灵境》微信小程序端需求规格说明书 (PRD)

## 1. 文档概述
* **产品名称**：心芒灵境（暂定）
* **定位**：基于心理学潜意识投射与 AI 赋能的日常心灵卡牌工具（规避封建迷信，主打心理自省与情绪疗愈）。
* **技术栈**：Uni-app (Vue3) / 原生微信小程序、Canvas 2D、Pinia。
* **核心目标**：提供丝滑的 3D 抽牌体验、打字机流式解读以及低门槛的裂变分享与激励广告闭环。

---

## 2. 功能模块与业务流程

### 2.1 身份与能量资产模块
1. **静默登录**：
   * 打开小程序调用 `wx.login` 获取 Code，换取后台自定义 Token，存储本地 Storage。
   * 无需强制授权手机号或头像昵称，降低首次使用跳出率。
2. **能量状态展示**：
   * 首页顶部常驻能量指示器：
     * 今日免费未用：显示 `今日灵感点: 1/1`（高亮）。
     * 今日免费已用完：显示 `灵感待充盈 (点击补充)`。

### 2.2 抽牌交互核心流程
1. **意念/困惑输入**：
   * 提供“输入当下的困惑/心绪”（字数限制 50 字以内，选填）。
   * 提供快速选择标签：【自我探索】、【亲密关系】、【职场瓶颈】、【今日灵感】。
2. **能量扣减拦截**：
   * 点击“洗牌/抽牌”时，若当前可用能量（免费点 + 奖励点）等于 0，拦截抽牌动作并唤起【能量充盈弹窗】。
3. **3D 抽牌动效**：
   * 洗牌动效：卡牌聚拢并洗动（约 1.2 秒）。
   * 切牌震动：调用 `wx.vibrateShort({ type: 'light' })` 模拟物理触感。
   * 单牌翻转：卡牌执行 3D 沿 Y 轴翻转 180 度，展示牌面与正/逆位标识。

### 2.3 AI 流式解读与展现
1. **打字机流式渲染**：
   * 通过 `wx.request` 的 `enableChunked: true` 与服务端建立流式连接。
   * 依次展现：
     * **能量标签**（如：破茧成蝶、蓄势待发）
     * **牌面意象投射**（基于心理学解释当下困惑）
     * **思维盲区与转念**
     * **今日正念行动建议**
2. **状态容错**：若流传输中断，支持点击“重新获取解析”。

### 2.4 裂变与补充能量闭环
1. **能量充盈弹窗（弹窗触发卡点）**：
   * 提示文案：“今日灵感已消耗完毕，传递能量给好友，即可重新充盈灵感点。”
   * **途径一（分享好友）**：点击绑定 `open-type="share"` 原生按钮。
   * **途径二（激励视频广告）**：根据后台返回的 `ad_reward_enabled` 开关动态展示“静心观看 15 秒短片获得 1 点”。
2. **分享卡片自定义 (`onShareAppMessage`)**：
   * 自动携带参数：`path: /pages/index/index?inviter_id=${currentUserId}`。
   * 动态文案：`我在心芒灵境抽取了【${cardName}】，来看看你的今日潜意识灵感`。
3. **受邀进入逻辑 (`onLoad`)**：
   * 检测 URL 中的 `inviter_id`，调用后台接收邀请接口为邀请人补能，并弹窗提示“已与好友产生心灵共鸣”。

### 2.5 朋友圈能量海报生成
1. **Canvas 2D 离屏绘制**：
   * 规格：750 × 1334 像素。
   * 内容要素：神秘感渐变底色、今日抽中卡牌插画、AI 提炼的 4 字能量词、核心正念金句、用户昵称、带参数的小程序菊花码。
2. **保存与分享**：
   * 一键保存图片至手机系统相册（处理相册授权弹窗被拒的兼容引导）。

---

## 3. 页面结构清单

| 页面路由 | 页面名称 | 核心交互与要素 |
| :--- | :--- | :--- |
| `/pages/index/index` | 灵境主台 (首页) | 能量指示条、困惑输入框、3D 卡牌区、洗牌/翻牌操作区、能量充盈弹窗 |
| `/pages/reading/result` | 能量解析页 | 卡牌高保真大图、流式打字机文字区、重抽按钮、生成海报按钮 |
| `/pages/poster/view` | 朋友圈海报预览页 | Canvas 渲染完成的图片预览、保存到相册按钮、返回首页 |
| `/pages/profile/history` | 往日回响 (历史记录) | 历史翻牌记录瀑布流、按日期归档查看历史建议 |

```

---

# 文件二：`BACKEND_PRD.md`（服务端需求文档）

```markdown
# 《心芒灵境》服务端架构与需求规格说明书 (PRD)

## 1. 文档概述
* **定位**：支撑小程序端轻量化高并发、大模型流式网关、防刷裂变计数以及运营后台的数据管理。
* **技术栈**：Node.js (NestJS / Fastify / Express) + Prisma ORM + Redis + PostgreSQL/MySQL。
* **核心目标**：保证大模型 SSE 流式输出的稳定性、守住免费/裂变防刷底线、确保微信敏感内容安全。

---

## 2. 核心功能与业务逻辑

### 2.1 用户鉴权与每日免费刷新逻辑
1. **微信静默登录换取 Token**：
   * 接收客户端传来的 `wx.login` 的 `code`。
   * 请求微信官方接口 `https://api.weixin.qq.com/sns/jscode2session` 换取 `openid` 与 `session_key`。
   * 用户表查询，若无则自动注册，颁发服务端 JWT。
2. **每日免费重置策略（懒加载判断）**：
   * 用户表维护 `last_free_date`（格式：`YYYY-MM-DD`）与 `bonus_energy`（奖励点数）。
   * **扣减优先级**：
     * 若 `last_free_date != today`：本次抽牌标记为消耗免费点，更新 `last_free_date = today`，允许抽牌。
     * 若 `last_free_date == today` 且 `bonus_energy > 0`：扣减 `bonus_energy = bonus_energy - 1`，允许抽牌。
     * 否则：拒绝抽牌请求，返回 `403 ENERGY_EXHAUSTED` 错误码。

### 2.2 裂变互惠与防刷机制
1. **裂变上报拦截（`POST /api/v1/share/accept`）**：
   * 校验入参 `inviter_id`（邀请人）与当前请求者上下文中的 `current_user_id`。
   * **防作弊校验**：
     * 禁止自己点击自己生成的链接 (`inviter_id === current_user_id`)。
     * 单日上限控制：Redis 记录 `INCR share_reward:${inviter_id}:${today}`，每日单人最多通过分享获取 3 点。
     * 数据库记录建立唯一索引 `(inviter_id, invitee_id, date_str)`，杜绝好友同日反复进出重复加点。

### 2.3 微信内容安全审查机制 (必做)
* 用户发起抽牌前若携带了自定义“困惑文本”，必须异步/同步调用微信官方 `POST wxa/msg_sec_check` 接口。
* 若命中违规，直接拦截并返回友好错误：“输入内容包含不适宜词汇，请重新表述你的困惑”。

### 2.4 AI 心理模型流式中转服务
1. **Prompt 组装网关**：
   * 读取后台动态配置的 System Prompt。
   * 注入严格合规规则：严禁断言未来吉凶、严禁封建迷信词汇、仅从心理投射与行动建议阐述。
2. **SSE 流式传输（Server-Sent Events）**：
   * 与大模型服务商（如 DeepSeek / GLM）建立流式传输。
   * 响应头设置 `Content-Type: text/event-stream; charset=utf-8` 与 `Transfer-Encoding: chunked`。
   * 分片解析为统一 JSON 协议推给微信小程序端。

### 2.5 预留激励广告服务端回调验证 (Server-to-Server)
* 提供标准 Webhook 接口接收微信广告服务器发送的验证参数（`trans_id`, `user_id`, `reward_time` 等）。
* 校验签名合法后，幂等性为用户增加 1 点奖励能量。

---

## 3. 核心 API 接口清单

| 方法 | 路径 | 鉴权 | 说明 |
| :--- | :--- | :--- | :--- |
| `POST` | `/api/v1/auth/wx-login` | 否 | 凭 `code` 获取 JWT 及用户资产概况 |
| `GET` | `/api/v1/user/profile` | 是 | 获取用户当前可用免费点与奖励点数 |
| `POST` | `/api/v1/share/accept` | 是 | 受邀用户进场，核验裂变关系并为邀请人补能 |
| `POST` | `/api/v1/tarot/draw` | 是 | 验证能量扣除、随机取牌、返回单牌信息与记录 ID |
| `GET` | `/api/v1/tarot/stream/:reading_id` | 是 | SSE 接口，流式返回大模型心理投射解读内容 |
| `POST` | `/api/v1/ad/reward-callback`| 微信验签 | 预留微信激励视频完播服务端异步回调 |
| `GET` | `/api/v1/tarot/history` | 是 | 分页拉取用户的历史翻牌记录 |

---

## 4. 数据库实体设计 (ER 简图)

```prisma
// 用户表
model User {
  id              String         @id @default(uuid())
  openid          String         @unique
  bonus_energy    Int            @default(0)   // 分享/看广告获得的额外能量
  last_free_date  String?                       // 上次消耗免费能量的日期 "YYYY-MM-DD"
  created_at      DateTime       @default(now())
  
  readings        TarotReading[]
  invitedRecords  Invitation[]   @relation("Invited")
}

// 裂变行为表
model Invitation {
  id          String   @id @default(uuid())
  inviter_id  String
  invitee_id  String
  date_str    String   // 格式 "YYYY-MM-DD"
  created_at  DateTime @default(now())

  @@unique([inviter_id, invitee_id, date_str]) // 防止重复加点
}

// 抽牌记录表
model TarotReading {
  id             String   @id @default(uuid())
  user_id        String
  card_id        Int
  card_name      String
  is_reversed    Boolean  @default(false)
  user_question  String?
  reading_result Json?
  created_at     DateTime @default(now())

  user           User     @relation(fields: [user_id], references: [id])
}

```

```

---

# 文件三：`ADMIN_PRD.md`（运营管理后台需求文档）

```markdown
# 《心芒灵境》Web 运营管理后台需求规格说明书 (PRD)

## 1. 文档概述
* **定位**：为运营人员和开发者提供全平台系统参数配置、AI Prompt 编排、数据漏斗观测与用户风控的后台管理系统。
* **技术栈**：Vue 3 + Vite + Element Plus + Pinia + ECharts。
* **核心目标**：实现无需发版即可动态控制小程序业务开关、监控裂变效率并微调大模型回复质量。

---

## 2. 功能模块设计

### 2.1 数据概览仪表盘 (Dashboard)
1. **核心 KPI 卡片**：
   * 今日活跃用户 (DAU) 与累计用户数。
   * 今日抽牌总次数（免费抽牌数 vs 裂变补能抽牌数）。
   * 裂变系数（K-factor = 受邀进入用户数 / 分享卡片发出数）。
   * 当日大模型 Token 消耗量估算及成本折算。
2. **图表展示**：
   * 过去 30 天用户新增与留存趋势折线图。
   * 每日抽牌时间分布热力图（观测白领晚间高峰）。

### 2.2 业务策略中控台 (System Config)
提供动态表单配置系统关键运行参数，保存后即时写入 Redis：
1. **能量与裂变规则**：
   * `daily_free_limit`：每日自然日赠送免费次数（默认：`1`）。
   * `share_reward_limit`：单日分享最高获得能量上限（默认：`3`）。
   * `share_reward_enabled`：是否开启分享奖励补能（布尔开关）。
2. **商业化灰度开关**：
   * `ad_reward_enabled`：是否在小程序端显示“看激励视频补能”按钮（支持在审核通过后一键开启）。
   * `ad_unit_id`：微信激励视频广告位 ID。
3. **微信审核模式 (`is_in_review`)**：
   * 开关开启时：小程序端关闭所有自定义文本输入框，流式解读返回预设的纯正向文学鸡汤，避开微信人工机审拦截。

### 2.3 AI 模型与 Prompt 编排中心
1. **大模型网关参数配置**：
   * 服务商切换下拉选框（DeepSeek / 智谱 GLM / MiniMax / 聚合中转）。
   * API Key、Base URL、Model Name 输入配置。
   * 模型温度（Temperature）、Top_P、Max_Tokens 滑块配置。
2. **Prompt 模板版本控制**：
   * System Prompt 编辑框（带高亮）。
   * 内置占位符说明：`{card_name}` (卡牌名称)、`{orientation}` (正逆位)、`{question}` (用户输入)。
   * **在线调试沙盒**：在后台直接模拟抽牌并触发模型输出，测试打字机输出流畅度及是否存在违禁词。

### 2.4 内容与风控管理
1. **卡牌库维护**：
   * 查看 78 张卡牌的中文名、英文名、预设心理学关键词及 CDN 插画预览。
2. **违禁词词库配置**：
   * 自定义敏感词字典（如迷信类词汇：改运、断命、看相等），前端命中时直接弹窗提示，不向后端发出请求。
3. **用户管理**：
   * 列表查看用户 OpenID、注册时间、当前能量余额。
   * 动作：手动补充/扣除能量、封禁作弊账号（黑名单屏蔽请求）。

---

## 3. 页面结构与路由规划

```text
/admin
├── /login              # 管理员账号密码登录页
├── /dashboard          # 核心运营与裂变数据指标总览
├── /strategy           # 业务策略配置 (开关、能量上限、审核模式)
├── /ai-orchestration   # Prompt 模板设计、模型网关与沙盒测试
├── /cards              # 78 张卡牌基础档案与资源库
├── /sensitive-words    # 敏感词过滤库管理
└── /users              # 用户列表、日志与能量状态管理

```

```

---

### 建议落地次序
1. **阶段 1（前后台打通）**：先根据 `BACKEND_PRD.md` 搭建 Node.js 基础工程，调通微信 `wx-login` 与大模型 SSE 流式输出接口。
2. **阶段 2（小程序呈现）**：对照 `MINIPROGRAM_PRD.md` 实现 3D 抽牌翻转交互、流式接收文本并跑通分享参数传递。
3. **阶段 3（审核与上线）**：利用 `ADMIN_PRD.md` 中的“审核模式开关”和“敏感词过滤”，提交微信官方审核；过审后动态放开 AI 自定义输入与后续广告逻辑。

```